# POCTIFY Governance Questionnaire

This project contains a React frontend and Netlify Function backend for a slide-style questionnaire.

## Development

```
npm install
npm run dev
```

## Deployment

Deploy on Netlify. Functions reside in `netlify/functions`. Set environment variables `SENDGRID_API_KEY` and `DESTINATION_EMAIL` for email delivery.
